<nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
    <div class="container-fluid">

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
            <ul class="navbar-nav flex-grow-1">



                <li class="nav-item">
                    <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="IndexIncidentWorker">Заявки</a>
                </li>


            </ul>
        </div>
    </div>
</nav>

@model ServiceManagerFire.Models.Incident

@{
    ViewData["Title"] = "DetailsIncidentWorker";
}

<h2>Просмотр заявки</h2>

<div>
  
    <table class="table">
        <tr> <th>Объект</th><th>Адрес</th><th>Описание заявки</th><th>Ответственный сотрудник</th><th>Статус заявки</th><th>Дата создания</th><th>Заказчик</th><th>Контакты</th><th>Город</th></tr>
        <tbody>

            <tr>
                <td>@Model.Objekt.Name.ToString() </td>
                <td>@Model.Objekt.Address.ToString() </td>
                <td>@Model.Description.ToString() </td>
                <td>@Model.User.Name.ToString() </td>
                <td>@Model.Status.Name.ToString() </td>
                <td>@Model.DateTime.ToString() </td>
                <td>@Model.Objekt.Customer.Name.ToString() </td>
                <td>@Model.Objekt.Customer.Description.ToString() </td>
                <td>@Model.Objekt.Customer.City.ToString() </td>

            </tr>

        </tbody>
    </table>
</div>
<div>
    <a asp-action="EditIncidentWorker" asp-route-id="@Model?.Id">Изменить</a> |
    <a asp-action="IndexIncidentWorker">Вернуться к списку заявок</a>
</div>

